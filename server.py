import socket

class Server:
    def __init__(self, ip, port):
        self.ip = ip
        self.port = port
    def listen(num, ):
        pass
# s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)